class Post {
    constructor(id, imageUri, talentId, profileImageUri, createdAt, updatedAt) {
      this.id = id;
      this.imageUri = imageUri;
      this.talentId = talentId;
      this.createdAt = createdAt;
    }
  }
  
  module.exports = Post
  